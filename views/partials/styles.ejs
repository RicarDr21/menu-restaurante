<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: 'Segoe UI', sans-serif; background: #fdfaf6; color: #333; }
  .site-header { background: #8b3a3a; color: white; padding: 1.5rem 2rem; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; }
  .site-header h1 { font-size: 1.5rem; }
  .site-header nav a { color: white; text-decoration: none; margin-left: 1rem; }
  main { max-width: 900px; margin: 2rem auto; padding: 0 1.5rem; }
  .site-footer { background: #333; color: #ccc; text-align: center; padding: 1rem; margin-top: 3rem; font-size: 0.85rem; }

  .listado h2 { margin-bottom: 1.5rem; color: #8b3a3a; }
  .grid { display: flex; flex-wrap: wrap; gap: 1.5rem; }
  .plato-card { background: white; border-radius: 10px; padding: 1.5rem; width: 250px; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
  .plato-card h3 { margin-bottom: 0.3rem; }
  .categoria { color: #888; font-size: 0.85rem; margin-bottom: 0.5rem; }
  .precio { font-weight: bold; color: #8b3a3a; margin-bottom: 1rem; }
  .plato-card a { display: inline-block; background: #8b3a3a; color: white; text-decoration: none; padding: 0.5rem 1rem; border-radius: 6px; font-size: 0.9rem; }

  .detalle { background: white; border-radius: 10px; padding: 2rem; box-shadow: 0 2px 6px rgba(0,0,0,0.1); }
  .volver { display: inline-block; margin-bottom: 1rem; color: #8b3a3a; text-decoration: none; }
  .detalle h2 { margin-bottom: 0.3rem; }
  .detalle .categoria { margin-bottom: 1rem; }
  .descripcion { margin-bottom: 1rem; line-height: 1.5; }
  .detalle .precio { font-size: 1.3rem; margin-bottom: 1rem; }
  .badge { display: inline-block; padding: 0.4rem 0.8rem; border-radius: 6px; font-size: 0.9rem; margin-bottom: 1.5rem; }
  .economico { background: #d4edda; color: #155724; }
  .medio { background: #fff3cd; color: #856404; }
  .premium { background: #f8d7da; color: #721c24; }
  .chef { border-top: 1px solid #eee; padding-top: 1rem; display: flex; flex-direction: column; gap: 0.3rem; }
</style>